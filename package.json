{
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix front-end && npm run build --prefix front-end",
    "start": "npm start --prefix front-end && npm start --prefix server"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
